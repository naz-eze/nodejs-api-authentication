# Node.JS API Authentication with JWT + Express + MongoDB

Uses Express framework, MongoDB as the database. 
Also uses mongoose to create models, bcryptjs to hash passwords, and jwt to create and verify tokens.